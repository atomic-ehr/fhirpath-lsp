{
  "name": "fhirpath-lsp-shared",
  "version": "0.1.0",
  "description": "Shared types and utilities for FHIRPath LSP",
  "main": "./out/index.js",
  "types": "./out/index.d.ts",
  "scripts": {
    "compile": "tsc -b",
    "watch": "tsc -b -w"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3"
  }
}